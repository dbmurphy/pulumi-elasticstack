name: elasticstack-example-python
runtime: python
description: Example Pulumi program using the elasticstack provider (Python)
config:
  elasticstack:elasticsearch:endpoints:
    value: '["http://localhost:9200"]'
  elasticstack:elasticsearch:username:
    value: elastic
  elasticstack:elasticsearch:password:
    secret: true
